package utils

import "fmt"

// SaveSensor : save data of sensors to sensor.yml
func SaveSensor(ohmdata OhmData) {
	fmt.Println("Saving sensor.yml ...")
	// tmp := ohmdata
	// sensor :=
	// sensorYaml, err:= yaml.Marshal()
}
